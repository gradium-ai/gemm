[package]
name = "xn-gemm-f64"
version = "0.20.1"
edition = "2021"
authors = ["sarah <>"]
description = "Fork of the gemm matrix multiplication crates, with a parallel gemv and kc/L1 cache blocking, published for the xn inference framework."
readme = "../README.md"
repository = "https://github.com/gradium-ai/gemm/"
license = "MIT"
keywords = ["linear-algebra"]

[dependencies]
raw-cpuid = { workspace = true }
seq-macro = { workspace = true }
dyn-stack = { workspace = true, default-features = false }
num-traits = { workspace = true, default-features = false }
num-complex = { workspace = true, default-features = false }
paste = { workspace = true }

gemm-common = { package = "xn-gemm-common", version = "0.20.1", default-features = false, path = "../gemm-common" }

[features]
default = ["std", "rayon"]
std = ["dyn-stack/std", "gemm-common/std"]
x86-v4 = ["gemm-common/x86-v4"]
experimental-apple-amx = ["std", "gemm-common/experimental-apple-amx"]
rayon = ["std", "gemm-common/rayon"]
